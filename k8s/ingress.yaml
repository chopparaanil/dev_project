apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: student-ingress

spec:
  ingressClassName: nginx

  rules:
  - host: student.local

    http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: student-service

            port:
              number: 80